# Changelog — integrate-themed skill

## 1.1.0 — 2026-04-18

Initial release.

- Full `ThemeManager` API reference
- Installation guide for vanilla, React, and Vue 3
- AI generation patterns including Chrome Extension proxy
- Custom AI providers: `provider: 'custom'` (OpenAI-compatible endpoint with `transformRequest`/`extractContent` overrides) and fully custom `AIProvider` instance
- CSS variable naming reference and usage examples
- Custom theme creation
- Integration checklist and common pitfalls
